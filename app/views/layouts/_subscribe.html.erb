<section id='subscribe' class='border'>
	<span>Get updates from Preston!</span>
	<%= simple_form_for :user, url: users_path do |f| %>
	    <%= f.input :email, placeholder: 'johndoe@example.com', label: false, input_html: {class: 'bad', style: 'width: 150px'} %>
	    <%= f.button :submit, 'Subscribe!', class: 'btn btn-primary', id: 'subscribe-button' %>
	<% end %>
</section>